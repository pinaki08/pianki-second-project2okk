export function areaOfRectangle(length, width) {
  const area = length * width;
  return area;
}

export function areaOfcircle(radious, pie) {
  const areaNew = radious * radious* pie
  return areaNew.toFixed(2);
}
